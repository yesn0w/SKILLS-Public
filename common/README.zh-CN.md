英文版本：[README.md](README.md)。

# 通用资产

这个目录只保留真正跨 Agent 的通用资产。

这个仓库目前没有通用 skills。辅助脚本保留在引用它们的 Codex 和 Claude skill 包内，
让每个安装后的 skill 保持自包含，并确保相对路径继续可用。

只有在同步更新 skill 文档和安装逻辑、明确以跨 Agent 通用内容使用时，才把资产移动到这里。
